{
  "name": "edge-viewer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp src/index.html dist/",
    "start": "echo 'serve from dist'"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}
